## Colleciton if possibly useful input from the domain specialists

### Mike M on 20260203

"We have talked about the scientific value of pulling in other tracers (nutrients, TA, Ba etc), but also accept that we don't have scope to do that straight away in AISIT.  Would it be possible to make the AI Challenge address this, to some extent? i.e. if there is a separate pre-existing dataset for e.g. nutrients or TA (made by someone else), could we make an AI algorithm consider both that and the AISIT database concurrently, and derive more information on the freshwater sources than either alone would permit?  The databases would have different spatial/temporal sampling, so it would have to be a clever AI - but that is the point of AI, no?"  
